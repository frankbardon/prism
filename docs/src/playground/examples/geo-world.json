{
  "$schema": "urn:prism:schema:v1:spec",
  "title": "World map (Natural Earth projection)",
  "description": "Geoshape mark over the embedded geodata catalog. data.feature_collection synthesizes one row per feature in the requested tier; the 'feature' channel binds the 'id' column the synthesized table carries. Try switching projection.type to 'mercator', 'equirectangular', or 'orthographic' to see the same data through a different lens.",
  "data": {"feature_collection": {"tier": "world-110m"}},
  "mark": "geoshape",
  "projection": {"type": "naturalearth"},
  "encoding": {
    "feature": {"field": "id", "type": "nominal"}
  }
}
