# CLAUDE.md

## Project Overview

Prism is a visualization library for materialized tabular data. Ships as a Go library (`github.com/frankbardon/prism`) and a CLI binary (`cmd/prism/`). Library is primary; CLI is a thin adapter.

**Design principles:**

- **Library-first.** Every public surface in `spec/`, `validate/`, `plan/`, `compile/`, `encode/`, `render/`, `resolve/`, `theme/`, `rpc/`, and `mcp/` is reachable as a Go API. `cmd/prism/` (host CLI) and `cmd/prismwasm/` (browser entry, `//go:build js && wasm`) never contain business logic — parse flags / marshal JSON, construct library objects, format output.
- **Six-stage pipeline.** Spec (JSON) → Validate → Plan → Compile → Encode → Render → Bytes. Each stage is independently testable, and intermediate artifacts (Plan, Scene IR, Encoded bytes) are stable JSON shapes downstream consumers can pin.
- **Vega-Lite vocabulary, snake_case keys.** Single-word terms (`mark`, `encoding`, `transform`, `layer`, `facet`, `concat`, `repeat`) match Vega-Lite verbatim. Multi-word keys are snake_case throughout (`stroke_width`, `corner_radius`, `font_size`).
- **Structured, expression-free transforms.** Prism has NO expression language. `filter` predicates, `calculate` derived columns, and condition `test` clauses are structured JSON built-ins (`spec/predicate.go` + `spec/calc_expr.go`): comparison / set / range / null-check leaves plus `and`/`or`/`not` combinators for filters; `op`/`fn`/`concat`/`case` nodes for calculate. A raw string where a predicate or expression is expected is rejected at decode. No `datum.` prefix, no operators, no JS function calls, no Vega expression eval. Anything richer is precomputed upstream by the caller.
- **No-execute predict & validate.** `validate/` reads only the spec + optional schema (no row I/O); `plan` builds the DAG without executing it. Network and filesystem I/O happen only at `plan.Execute` time.
- **Pulse-free — consumes materialized data.** Prism has NO dependency on `github.com/frankbardon/pulse` and never reads `.pulse` files (the dependency was evicted in epic E4; a `go list -deps` firewall keeps it out — see Non-Skippable Gates). Prism renders **already-materialized rows**: inline `data.values` / `datasets.*.values`, or lazily via a caller-supplied `resolve.DataResolver` (`data: {ref}`). The downstream/consuming library owns 100% of Pulse — it runs the Pulse query and converts the response into a Prism spec with rows inlined. Every transform (including aggregates, `crosstab`, and `regression`) executes in-memory over `table.Table` in `compile/inmem/`; the aggregate aliases (`count`, `sum`, `mean`, …, `wmean`, `ratio`, `lift`, `share`, `ci0`, `ci1`) are all client-side computations with no backend op constant.

## The Update Demand

Any change to Prism code, configuration, spec vocabulary, schema bundle, or public surface MUST update the corresponding doc page(s) and CLAUDE.md in the same PR.

| If you change... | You MUST also update... |
|---|---|
| A mark in `encode/marks/` | `docs/src/concepts/marks.md` + add a gallery entry under `docs/src/gallery/<family>/` if user-visible |
| An encoding channel | `docs/src/concepts/encoding.md` + `schema/v1/` JSON Schema for the channel shape |
| Block-level axis / legend suppression (`"axis": null`, `"legend": null`) | `spec/encoding.go` — `PositionChannel` / `MarkChannel` decode the key **tri-state**: absent (`Axis`/`Legend` nil, `AxisHidden`/`LegendHidden` false) vs configured vs explicit null (nil pointer **plus** the Hidden flag). A nil pointer alone does NOT mean hidden; every reader must consult the flag, and the paired `MarshalJSON` re-emits the null so a round-trip cannot collapse the two states. Encode side: `encode/layout.go` (`AxisPlacement.XHidden`/`YHidden` — `Sides()` skips a hidden axis so its side's padding reservation is released and the plot expands), the `axisHidden` / `legendHidden` / `placementFor` / `layerAxisHidden` helpers + axis / legend guards in `encode/encode.go`, plus `encode/encode_composite.go` (layers share one pair of axes — suppression must be unanimous) and `encode/encode_facet.go` (shared axis follows the child spec). Schema: `schema/v1/axis.schema.json` + `legend.schema.json` (`"type": ["object", "null"]`). Docs: `docs/src/concepts/encoding.md`. This is a DIFFERENT mechanism from `axis.title: false` (`axisTitleString` in `encode/encode.go`, which drops only the title) — do not merge the two, and do not extend `false` to the block level |
| A discrete **grouping** channel (`color`, `detail`), or the row partitioner behind it | `encode/marks/group.go` — `groupChannels` lists the bound grouping channels and `groupRows` partitions on the tuple. There is exactly ONE partitioner; extend it, never add a parallel `groupRowsBy<Channel>` — line/area (and, from E5-S2, stacking) all share it. Plus the `marks.Inputs` field, the plumbing in `encode/encode.go` + `encode/encode_composite.go`, the implicit group-by in `plan/build/build.go`'s `injectEncodingAggregate` (a grouping field must survive the synthetic aggregate), `validate/rules/channel_for_mark.go`, `schema/v1/encoding.schema.json`, and `docs/src/concepts/encoding.md`. Only `color` consumes a palette / builds a legend; `detail` is grouping-only and must leave styling untouched |
| A **span** channel (`x2`, `y2`) — which marks draw one, or how | `encode/marks/span.go` — `spanBound` / `spanPixels` / `rectAxisExtent` are the single source of span geometry; `encode/span.go` — `spanDomainValues` widens the base channel's domain and `spanChannel` binds the companion to the base channel's resolved scale. A span channel NEVER resolves a scale of its own. Plus the per-mark entry points (`encode/marks/bar.go`, `rect.go`, `rule.go`, `area.go`), the `marks.Inputs.X2`/`Y2` plumbing in `encode/encode.go` + `encode/encode_composite.go` (including `collectLayerDomains`) + `encode/encode_facet.go` (`buildSharedScaleForFacet`), the `spanCapableMarks` table in `validate/rules/span_channel_walk.go` (a mark that cannot draw a span must REJECT it — `PRISM_SPEC_042` — never ignore it), `schema/v1/encoding.schema.json`, and the Span-channels section of `docs/src/concepts/encoding.md`. Vega-Lite's `theta2` / `radius2` are intentionally absent from the schema and the spec types |
| A transform (`filter`, `aggregate`, `bin`, `calculate`, `join`, `pivot`, `sample`, `sort`, `unpivot`, `window`, `crosstab`, `regression`, `timeunit`, `stack`) | `docs/src/concepts/spec.md` (transform section) + add a Plan node under `plan/nodes/` + add a `Spec*Transform` union variant in `spec/transform_union.go` + register `transformDiscriminators` + `transformAsName` switch in `plan/build/build.go` + JSON Schema variant in `schema/v1/transform.schema.json` (`oneOf` entry + `$def`). The `filter` predicate grammar lives in `spec/predicate.go` and `calculate` in `spec/calc_expr.go` (structured built-ins, no expression string) — extending either operator/function set also touches those types, the `predicate_*` / `calc_expr` `$defs` in `schema/v1/transform.schema.json`, the `compile/inmem/filter.go` / `calculate.go` evaluators, and the `PRISM_SPEC_037` / `PRISM_SPEC_038` validate rules |
| **Mark orientation** (`mark.orient`) — which marks read it, or how a category/measure axis is chosen | `encode/marks/orient.go` — `Orientation` + `MarkOrientation` (band-required inference) / `MarkOrientationOr` (band-optional, for a mark whose category axis may be continuous — `area`, `tick`, the sparks) + `CategorySlots` / `CategoryCenters` / `BaselinePixel` / `MeasureSpans` / `MeasureDirection` / `CategoryChannel` / `MeasureChannel` / `PlotCategoryExtent` / `PlotMeasureExtent` / `OrientedRect` / `OrientedPoint` are the single source of oriented geometry. A baseline-anchored or band-seated mark must resolve orientation ONCE at the top of its encoder and read every coordinate through those helpers; it must NOT re-derive "x is the band axis" inline, must NOT call `BandWidth()` directly, and must NOT add a second orientation field (`bullet`'s pre-existing `orientation` is the one documented exception — different default, whole-mark rotation rather than an axis swap). Band slots go through `rectAxisExtent` (`encode/marks/span.go`), the ONE normaliser for a signed band step — a y band scale runs bottom-to-top and its `BandWidth()` is negative, so reading it straight into `RectGeom.H` yields an undrawable rect; `heatmap` was folded onto it in E9-S2, so no copy of that normalisation survives. Converted encoders: `bar.go`, `rect.go`, `area.go`, `tick.go`, `boxplot.go`, `violin.go`, `winloss.go`, `adornment.go` (the spark series points + reference band). Plus `spec/mark.go` (`MarkDef.Orient`), `schema/v1/mark.schema.json` (`orient` enum — it carries the full vocabulary, `radial` included, so `PRISM_SPEC_046` answers with a real message instead of an opaque enum miss), the `orientAwareMarks` table in `validate/rules/mark_orient.go` (a mark that draws no orientation must REJECT `orient` — never ignore it), `errors/codes.go`, `validate/RULES.md`, and the Orientation section of `docs/src/concepts/marks.md` (including the inference table AND the per-mark support table). Stacking (E5-S2) swaps the anchor by replacing `BaselinePixel`, not by branching on the axis again. |
| The **order** channel (`encoding.order`), or anything that changes row order | `spec/order.go` — `ResolveOrder` is the single decision point, called by BOTH `plan/build/build.go`'s `injectEncodingOrder` and `encode/encode.go` / `encode_composite.go` (which set `marks.Inputs.Ordered`), so plan and encode can never disagree. **Row order is the ONE mechanism behind all three Vega-Lite senses** — stack order (`plan/nodes/stack.go` ranks segments by first appearance), draw order (per-row marks are emitted in table order), and path point sequence (`groupRows` hands each group its indices in table order). Do NOT add a second reordering path at encode; the only encode-side code is the `Ordered` flag that suppresses line/area's default x-sort (`encode/marks/line.go`, `area.go`). The injected SortNode sits **after** `injectEncodingAggregate` and **before** `injectEncodingStack` — that placement is what makes an order key able to name an aggregated output column and still reach the stack. A non-aggregated order field joins the synthetic aggregate's group-by (like `detail`); a field another channel already aggregates pointedly does NOT widen it. Plus `validate/rules/order_channel_shape.go` (`PRISM_SPEC_055`) + `order_channel_aggregate.go` (`PRISM_SPEC_056` — `aggregate` on an order entry is rejected, not honoured), `errors/codes.go`, `schema/v1/encoding.schema.json` (`order_channel`), and `docs/src/concepts/encoding.md` (Order channel). **`order` unbound must reorder nothing** — `ResolveOrder` returns nil, no node is injected, and every pre-E5-S4 chart stays byte-identical |
| A sort direction anywhere on the wire (`sort` / `window` transform `order`, `encoding.order` `sort`) | `spec/order.go` — `SortDirectionDescending` / `SortDirectionValid` are the ONLY readers of the vocabulary; `plan/nodes.SortKey.Descending()` delegates to them and `compile/inmem/sort.go` + `window.go` call that, never a string compare. Canonical spellings are `ascending` / `descending`; `asc` / `desc` are aliases. Every enum that names them must list all four — `schema/v1/transform.schema.json` (sort + window `order`) and `schema/v1/encoding.schema.json` (`order_channel` `sort`). A schema enum that disagrees with the code is exactly how `"descending"` shipped as a silent no-op until E5-S4 |
| **Stacking** — the `stack` key on a position channel, the implicit bar/area default, or the accumulation itself | `spec/stack.go` — `ResolveStack` is the SINGLE decision point, called by BOTH `plan/build/build.go` (`injectEncodingStack`, which appends the `StackNode` after the synthetic encoding aggregate) and `encode/stack.go` (`rebindStack`, which repoints the stacked channel onto the node's `<field>_start` / `<field>_end` columns). Never re-derive "does this stack?" anywhere else — the two stages must reach the same verdict from the same spec, and there is no plan → encode side channel. Plus `plan/nodes/stack.go` + `compile/inmem/stack.go` (the accumulation), the `*nodes.StackNode` cases in `compile/inmem/backend.go` and `plan/passes/projection_pruning.go` (`collectColsFromNode` + `rewireSingleInput` — an unhandled node kind makes the pass bail conservatively for that source), the tri-state `Stack` / `StackNull` decode in `spec/encoding.go`, `schema/v1/encoding.schema.json` (`stack` enum) + `schema/v1/transform.schema.json` (`stack_transform`), and `docs/src/concepts/encoding.md` (Stacking) + `docs/src/concepts/spec.md` (Stack transform). `rebindStack` is called from exactly two places — the flat `encode.Encode` and the `liveChild` loop in `encode/encode_composite.go` — both BEFORE anything reads the encoding, which is what lets scale resolution, axis building and the span-aware bar / area encoders stay ignorant of stacking. The mark encoders need NO stack-specific code: a stacked mark is an ordinary `x`→`x2` / `y`→`y2` span. |
| An analytic transform that pivots/fits the whole materialized table (e.g. `crosstab`, `regression`) | New plan node under `plan/nodes/` (mirror `crosstab.go` / `regression.go` — computes over the upstream `table.Table` pure-Go in `compile/inmem/`, deriving its output schema at execute-time via `Schema([]*table.Schema)` like `GroupAggregateNode`) + matching spec transform variant + a **structural** validate rule (`*_structure.go` — shape checks only, e.g. cell aggregate / target + ≥1 predictor; NO position constraint) + `PRISM_SPEC_NNN` error code + plan-build dispatch in `plan/build/build.go` that routes the transform through the standard `resolveInput()` path like any other transform. These transforms accept **any upstream table** (a source ref, inline `data.values`, or the output of an earlier transform), so they compose anywhere in a chain — they are NOT first-transform-only. (Historical note: the retired `PRISM_SPEC_033` and `PRISM_PLAN_<NAME>_REQUIRES_SOURCE` codes enforced the old source-rooted / first-position model; they survive as `Retired code:` history entries in `errors/codes.go`. `PRISM_SPEC_035` was reworded to its structural-only meaning.) |
| A composition operator (`layer`, `concat`, `hconcat`, `vconcat`, `facet`, `repeat`) | `docs/src/concepts/composition.md` + composite encoder under `encode/encode_composite.go` + the operator probe in `validate/rules/composite_parent_encoding.go` (`compositeOperator` / `compositeChildNodes`, which mirror `plan/build.IsComposite` and must learn the new operator or `PRISM_SPEC_054` goes blind to it) |
| What a composition parent inherits to its children | `plan/build/composite.go` (`mergeParentDatasets` — the ONE inheritance point: `datasets`, `data` and `$schema`, nothing else) + `validate/rules/composite_parent_encoding.go` (`PRISM_SPEC_054`, which rejects an `encoding` block written beside a composition operator because nothing reads it) + `errors/codes.go` + `validate/RULES.md` + the "What a parent passes down" section of `docs/src/concepts/composition.md`. The composite encoder reads `child.Spec.Encoding` and never the parent's, so a key that is not in `mergeParentDatasets` is dead at the parent level — reject it at validate rather than letting the spec lie. Vega-Lite DOES inherit a parent `encoding` into layer children; implementing that is a legitimate future feature, and it would turn this rule off rather than extend it. The `spec` child of a `facet` / `repeat` parent is a child like any other — its own encoding is the chart that gets drawn and must stay legal |
| A scale type | `docs/src/concepts/encoding.md` (scale section) + `encode/scale/` implementation + tick generator under `encode/ticks*.go` + a `resolve<Type>` arm in `encode/scale.go` that takes `ScaleOpts` and honours `Domain` / `Zero` / `Nice` |
| A domain-shaping knob on the `scale` block (`domain`, `zero`, `nice`, `base`, `exponent`, …) | `spec/scale_axis_legend.go` (`Scale` struct) + the `ScaleOpts` field and `ScaleOptsFromSpec` in `encode/scale.go` + every `resolve*` arm that must honour it + `scaleFromUnified` / `sharedScaleOpts` in `encode/encode_composite.go` (the cross-layer shared path) + `schema/v1/scale.schema.json` + `docs/src/concepts/encoding.md` (the "Bounding the domain" section). `ScaleOpts` is threaded from exactly four places: `resolveChannel` (`encode/encode.go`), the facet shared-scale resolver (`encode/encode_facet.go`), `resolveSharedScale` (`encode/encode_composite.go`), and the table-cell sparkline path (`encode/table.go`, which opts out of `nice` on purpose; the domain-to-pixel knobs below default off, so it opts out of them by construction) |
| A **domain-to-pixel** knob on the `scale` block (`clamp`, `reverse`, `round`, `padding`, `padding_inner`, `padding_outer`, `align`) | the same chain as the domain-shaping row, plus the geometry itself. `clamp` / `round` live on `scale.ContinuousOutput` (`encode/scale/output.go`), which `LinearScale` / `LogScale` / `PowScale` embed and `SqrtScale` / `TimeScale` inherit by wrapping — never re-implement the policy per family. Band + point geometry is `encode/scale/band.go` / `point.go` (`layout()` is the single formula: step / offset / width, d3-scale shaped). Scales are built through `NewBandScale` / `NewPointScale` / `newLinearScale` / `newOrdinalScale` in `encode/scale.go` so the flat encoder and `scaleFromUnified` in `encode/encode_composite.go` cannot diverge. `reverse` is applied per family: continuous flips the pixel range (`ScaleOpts.pixelRange`), discrete reverses the **slot assignment** so the signed step and the negative `BandWidth()` an inverted y range produces both survive — flipping a band range instead would break every horizontal bar and heatmap row. Range violations are `PRISM_SPEC_049` (`validate/rules/scale_band_geometry.go`). The band defaults (inner 0.1 / outer 0.05 / align 0.5) are load-bearing: they reproduce the pre-split `step = span/n` layout byte-for-byte, so changing any of the three moves EVERY bar, tick, heatmap and boxplot golden |
| The **plot-region clip** — when out-of-domain marks are clipped, or what the clip covers | `encode/clip.go` is the single policy + assembly point (`wantsPlotClip` decides, `armPlotClip` writes, `renameScene` re-keys a cell's clip when the composite renames it, `offsetClips` moves it with the rest of the scene) + the three `scene.Scene` construction sites that call `armPlotClip` (`encode/encode.go` flat and `buildSceneDoc`, `encode/encode_composite.go` layer) + `scene.Scene.ClipRef` -> `scene.Defs.Clips` (Scene IR, additive) + `render/svg/renderer.go` (`clip-path` on the `prism-plot` group) + `spec/mark.go` `MarkDef.Clip` + `schema/v1/mark.schema.json` + `docs/src/concepts/encoding.md` (Rows outside the domain) + `docs/src/concepts/marks.md` (Clipping). The invariants: **the clip reaches the mark container ONLY** — axes, legends, the title and the above-marks `prism-axes` group are siblings of `prism-plot` and must stay unclipped — and **the default is armed only by an explicit `scale.domain` on a position channel**, which is what keeps every scene with a data-derived domain byte-identical. One clip per **Scene** (i.e. per grid cell), never per layer: layers share one plot rect, and `concat`/`facet`/`repeat` cells each get their own scene and decide independently. `scene.SceneLayer.Clip` is a legacy unused bool — do NOT repurpose it. The vendored JS renderer needs no change: it delegates SVG generation to the same Go renderer via WASM |
| A field on `encode.AxisOpts`, or a property on `spec.Axis` | `encode/encode.go`'s `axisOptsFor` (the single reader of `channel.axis`) + `encode/axis_build.go` (`DefaultAxisOpts` + the `BuildAxisWithOpts` consumer) + `schema/v1/axis.schema.json` + `docs/src/concepts/encoding.md`. **Every** `BuildAxisWithOpts` call site must reach the spec's per-channel block through `axisOptsFor` / `axisOptsForTitled` (flat + histogram) or `sharedAxisOpts` (shared layer / facet axes) — passing `DefaultAxisOpts` discards `axis` config, which is silent under the **default** (`shared`) resolve mode. `encode/axis_call_sites_test.go` fails the build on a new bypass; `BuildAxis` is the only allowlisted caller. `spec.Axis` properties are folded across composition children reflectively by `encode/axis_shared.go`'s `mergeSharedAxisSpec`, so a new property needs no registration there — but `encode/axis_shared_internal_test.go` drives every exported field through the merge and will flag it if the fold stops being exhaustive. A property whose effect is **rendered geometry** also needs a `scene.Axis` field (Scene IR, additive) + a `render/svg/axes.go` consumer — the three that exist today (`tick_size`, `label_padding`, `title_padding`) all resolve through the single `resolveAxisMetric(specVal, themeVal, builtin)` helper against a `theme*` reader (`themeTickSize` / `themeLabelPadding` / `themeTitlePadding`), and a fourth must join them there rather than growing its own chain; a geometry constant it adds (`defaultTitlePadding` + the `titleDrop*` allowances) must be chosen so the value every built-in theme states reproduces the previous hard-coded coordinate, which is what keeps the goldens still; one that changes **how much room the axis needs** also needs `AxisSideReserve` in `encode/layout.go` plus a `placement.ReserveFrom(...)` call at every layout site (`placementFor` in `encode/encode.go`, `specSharedAxisOpts` in `encode/encode_composite.go` + `encode/encode_facet.go`) so the padding and the geometry cannot disagree Tick / gridline selection (`tick_count`, `values`, `tick_min_step`) lives in `encode/axis_ticks.go`, whose `DefaultTickCount` replaced the hard-coded `5` — **grid lines are emitted at exactly the major tick positions** (`horizontalGrid`), so anything that reshapes the tick set reshapes the grid, and that coupling is intentional (no separate grid-values knob). `BuildAxisWithOpts` keeps a pinned five-argument shape because `axis_call_sites_test.go` asserts it, so axis-level warnings leave through the `AxisOpts.Warnings` sink instead of a return value: wire it at every call site with `.withWarnings(&warnings)` or the warning is silently discarded |
| Where an axis SITS (`axis.orient`) | `encode/layout.go` — `AxisPositionFor` is the ONE orient -> `scene.AxisPosition` mapping, and `AxisPlacement` (built from it) is the single source of truth for BOTH the padding reservation and the position stamped on the axis, so padding can never sit on one side while the axis renders on another. Resolved per path: `placementFor` (flat, reading orient back out of `axisOptsFor`), `sharedAxisPlacement` + `sharedAxisOrient` in `encode/axis_shared.go` (layer + facet, folding orient across children first-specified-wins off `mergeSharedAxisSpec` — it runs the fold early and DISCARDS its warnings, because `sharedAxisOpts` re-runs it at axis-build time and is the one that reports `PRISM_WARN_AXIS_CONFIG_CONFLICT`; a second reporter would double-warn). Only two of the four sides are legal per channel — `bottom`/`top` on x, `left`/`right` on y — enforced by `validate/rules/axis_orient_channel.go` (`PRISM_SPEC_044`), never by the encoder, which falls back to the default side to stay total. `render/svg/axes.go` already renders all four positions (tick direction, label anchor, title rotation); a facet's shared axis picks its anchor cell from the placement via `facetAnchorPlot`. Docs: the Axis placement section of `docs/src/concepts/encoding.md` |
| An axis **component switch** (`labels`, `ticks`, `domain`) or **geometry** knob (`tick_size`, `label_padding`, `label_limit`, `zindex`) | The `axisOptsFor` row above, plus: `scene.Axis.HideLabels`/`HideTicks`/`HideDomain` (stated as negations so an absent key keeps the component drawn and `omitempty` keeps existing goldens byte-identical) + `render/svg/axes.go`'s per-component guards. `label_limit` truncates at **encode** time in `applyLabelLimit` (`encode/axis_build.go`), never at render time, so every Scene IR consumer sees the already-shortened label; it shares the 6 px/char `axisLabelCharWidth` heuristic with `applyLabelOverlap` — Prism has no text-measurement pass. Since E4-S5 that constant aliases `scene.LabelCharWidth` (with `scene.LabelLineHeight` alongside it), the single estimate the graph marks' node labels read too; change it there, not in a local copy. `zindex` splits the axes into two `prism-axes` groups in `render/svg/renderer.go` (`renderAxesGroup`), the above-marks one emitted as a **sibling** of `prism-plot` so it stays outside the plot-region clip path (E2-S2 shipped that clip and re-verified the escape; `render/svg/clip_test.go` pins it); a grid-level shared axis (`renderSharedAxes`) always draws after the cells regardless — E2-S2 re-examined that deferral and kept it, because a shared axis lives outside every cell's `prism-plot` group and so is never clipped either way, leaving only a z-order gap whose fix would restack every layered/faceted golden. Component suppression releases layout padding through `AxisSideReserve` — the tick share and the label share of `layoutAxisReserve`; the domain line sits on the plot edge and reserves nothing. This is a DIFFERENT mechanism from `"axis": null` (E1-S4, whole-block), which emits no axis at all: with every component off the axis is still emitted and still draws its grid and title |
| A theme **geometry** token (one that moves SVG coordinates rather than restyling them — `axis.tick_size`, `axis.label_padding`) | The theme-token row below, **plus** a `scene.Theme` field + the copy in `theme/theme.go`'s `ToSceneTheme` + the consumer in `render/svg/`. A CSS variable cannot move a line endpoint, so a geometry token that lives only in `theme/css.go` is dead. Precedence is spec-over-theme-over-built-in, expressed once in `resolveAxisMetric` (`render/svg/axes.go`), and documented in BOTH `docs/src/concepts/encoding.md` (axis section) and `docs/src/concepts/themes.md` (Axis geometry precedence) so it is discoverable from either side. `theme.AxisStyle` and `spec.Axis` carry identically named `TickSize` / `LabelPadding` fields — check the receiver's type, never just the selector name |
| A scale type | `docs/src/concepts/encoding.md` (scale section) + `encode/scale/` implementation + tick generator under `encode/ticks*.go` |
| Legend placement — a `legend.orient` value, a `scene.LegendPosition`, or anything that changes how much room a legend claims | `encode/legend_build.go` (`LegendPlacement`, `ResolveLegendPlacement`, `IsSideLegend`, `LegendBox` + its `Size` / `SideExtent` methods, `placeLegendFrame`) + `encode/layout.go` (`LayoutSides.MarkLegend` / `SideChrome.LegendExtent` / `Padding.LegendBand`) + both `BuildSymbolLegend` call sites in `encode/encode.go` **and** the per-layer one in `encode/encode_composite.go` (via `layerLegendPlacements`) + `schema/v1/legend.schema.json` (`orient` enum) + `docs/src/concepts/encoding.md` (Legend placement section). The invariant to preserve: **side placements (`left`/`right`/`top`/`bottom`) reserve margin through `LayoutSides.MarkLegend` so the plot rect shrinks; corner placements (`top-left`…`bottom-right`) reserve nothing and overlay the plot.** A side reservation is measured from the real box (`LegendBox.SideExtent`), never from the `layoutLegendReserve` fallback constant, and is skipped when the channel has fewer than two categories — that is what keeps default (corner) placement byte-identical in the goldens. `LegendBox` is the single geometry seam: the emitted frame and the reserved band both go through it, so a change that alters a legend's size belongs there and nowhere else — `legendReserveBox` is the pre-layout measurement and `symbolLegendBox` / `gradientLegendBox` the two ways one is built. Legend geometry the SVG renderer consumes lives on `scene.Legend` (`Frame`, `Padding`, `Direction`); adding a field there also touches `render/svg/legends.go`. |
| Legend content — `legend.title` / `values` / `format` / `tick_count` / `label_limit`, or anything that changes what a legend *says* | `encode/legend_content.go` (`LegendContent` + `ResolveLegendContent` — the single reader of every `spec.Legend` field; `SelectCategories` / `Label` / `Truncate` / `MaxChars` are its whole surface) + the `Content` field on `LegendInputs` and its four fill sites (`encode/encode.go` ×2, `encode/encode_composite.go` via `layerLegend.content`, plus `legendReserveBox`, which must measure the **shown** entries so the reserved band matches what is drawn) + `schema/v1/legend.schema.json` + `docs/src/concepts/encoding.md` (Legend content section). Two rules: `legend.values` selects **indices**, never labels — a swatch colour is `palette[originalIndex % len(palette)]`, which is what keeps a filtered legend matching the marks — and `legend.format` routes through `encode/format` (the d3 subset `PRISM_SPEC_011` validates against), **not** `fmt.Sprintf`. `legend.title` uses the string-or-`false` convention `axisTitleString` owns; a nil `*spec.Legend` yields the zero `LegendContent`, which reproduces the pre-E3-S3 defaults byte-for-byte. |
| Legend **form** — `legend.type`, the symbol/gradient inference, or the gradient bar's wiring | `encode/legend_content.go` — `ResolveLegendKind` is the SINGLE decision point (`legend.type` overrides; otherwise a `quantitative` channel infers `LegendKindGradient` and everything else `LegendKindSymbol`) and `encode.DefaultLegendPosition` turns that kind into the default anchor. The kind must be resolved **before** `ResolveLegendPlacement`, because a gradient defaults to `scene.LegendRight` (a side placement that reserves margin) while a symbol legend keeps `scene.LegendTopRight` (a corner that overlays) — a 130-px bar cornered would lie over the heatmap cells it describes. `buildColorLegend` (`encode/legend_build.go`) is the one builder both encoders call: it emits the bar via `BuildGradientLegend` + a `scene.Gradient` the caller files through `registerSceneGradient`, and **falls back to `BuildSymbolLegend` when the bound column has no numeric extent** — the same `legendGradientViable` test `legendReserveBox` ran pre-layout, so the reservation and the built legend can never disagree. Stops come from `GradientStopsFromPalette(colorChannel.SequentialPalette)`, the same ramp `encode/marks/heatmap.go` interpolates within, so bar and cells always describe one ramp; the composite path resolves `SequentialPalette` for exactly that reason. Gradient ids are scene-qualified (`LegendGradientID`) and re-keyed by `renameScene` (`encode/clip.go`) alongside the plot clip, so a multi-cell grid never emits two `<linearGradient>` elements sharing an id. Plus `validate/rules/legend_type_coherent.go` (`PRISM_SPEC_051` — an override the channel cannot fill is REJECTED, never ignored), `errors/codes.go`, `validate/RULES.md`, `schema/v1/legend.schema.json` (`type` enum), and `docs/src/concepts/encoding.md` (Symbol or gradient). |
| Legend **layout** — `legend.direction`, `symbol_type`, `symbol_size` | `encode/legend_build.go` — `LegendBox.Direction` / `SymbolSize` are the only fields that change which way `Size()` grows, so the emitted frame AND the side reservation follow from one measurement; `symbolLegendBox` / `gradientLegendBox` are the two construction helpers every site goes through. `LegendContent.swatchFor` is the one place a `scene.SwatchSpec` is built (solid square by default, `scene.SwatchSymbol` + `Shape` once `symbol_type` names one). Scene IR: `scene.Legend.Direction` (`scene.LegendDirection`) + `scene.SwatchSpec.Shape` / `Size`, all `omitempty` so a default legend serialises byte-identically. Renderer: `render/svg/legends.go` derives the horizontal entry pitch from `Frame.W` (the same per-entry width `LegendBox.Size` used) and draws shaped swatches through **`render/svg/symbols.go`** — `symbolTag` + `writeSymbolGeom`, the ONE emitter for the `scene.PointShape` vocabulary, which `renderPoint` also calls. Never write a second shape emitter: a point's diamond and a legend swatch's diamond diverging is a visible bug. A circle still emits `<circle cx cy r>`; only the non-circle shapes become a `<path>`, which is what keeps every committed point golden byte-identical. The vocabulary itself is `scene.PointShapes`, read by both the encoder and `validate/rules/legend_symbol_type.go` (`PRISM_SPEC_052`). Plus `schema/v1/legend.schema.json` (`direction` / `symbol_type` enums) + `docs/src/concepts/encoding.md` (Legend layout). |
| A theme (or built-in theme value) | `docs/src/concepts/themes.md` + `theme/<name>.go` + register in `theme/registry.go` + token entry in `theme/css.go` + gallery fixture in `docs/src/gallery/themes/` + gallery index row in `docs/src/gallery/index.md` |
| A theme token (nested block field on `theme.Theme`) | `theme/style.go` (struct field) + matching spec wire field in `spec/theme.go` + override copy helper in `theme/override.go` + JSON Schema property in `schema/v1/theme.schema.json` + CSS variable emitter in `theme/css.go` + clone/merge in `theme/loader.go` + every built-in theme that should set the new token. An **axis** token is automatically per-axis-addressable — see the row below; nothing extra to register unless it is geometry |
| A **per-axis** theme block (`theme.axis_x` / `axis_y`) — the token set, or how it folds over the shared `axis` | `theme/style.go` — `Theme.AxisFor(channel)` is the ONE place the theme half of the precedence chain lives (it delegates to `mergeAxis` in `theme/loader.go`, the same per-property fold `axis` itself uses); never add a second resolver. The full chain is **spec channel `axis` block > `theme.axis_x`/`axis_y` > `theme.axis` > built-in default**, documented once in `docs/src/concepts/themes.md` (Per-axis blocks + Axis geometry precedence). Two transport routes, and which one a token takes is decided by what it does: a **colour / stroke / font** token rides the CSS cascade — `writeAxisScopeVars` (`theme/css.go`) re-emits the block scoped to `.prism-axis-x{…}` / `.prism-axis-y{…}` and custom-property inheritance performs the per-property merge, so no fold is computed and no new selector is added to `writeClassSelectors`; a token that moves **SVG coordinates** (`tick_size`, `label_padding`, `title_padding`) or is emitted as an **SVG attribute** (`label_line_height`, `label_letter_spacing`, `title_line_height`, `title_letter_spacing`, `filter`) cannot ride a variable and must go through `scene.Theme.AxisX`/`AxisY` (`scene.AxisTokens`, projected by `sceneAxisTokens` in `theme/theme.go`) and be read back in `render/svg/axes.go` via `themeTickSize` / `themeLabelPadding` / `themeTitlePadding` / `AxisTokensFor` — extend `resolveAxisMetric`'s theme arm, never add a parallel path. The spec half of the chain needs `spec.Axis` + `encode.AxisOpts` + `scene.Axis` to carry the same token (see the `encode.AxisOpts` row) — without it the theme block is the highest level an author can reach, which is the state `title_padding` was in before E8-S2. Plus `spec/composition.go` (`ThemeOverride.AxisX`/`AxisY`), `theme/override.go`, `theme/loader.go` `Merge` (per-axis blocks fold against their own counterpart, NOT into `axis`), `theme/theme.go` `Clone`, `theme/validate.go` (filter-ref check), `schema/v1/theme.schema.json` (`theme_override` is `additionalProperties:false`, so an unregistered key is a hard shape error). `x2`/`y2` resolve to their base channel's block. Emitting nothing when both blocks are nil is what keeps every committed golden byte-identical — preserve it |
| A named color scheme | `theme/schemes.go` (`builtinSchemes` entry — name, kind, hex list) + `docs/src/concepts/themes.md` scheme catalogue + (if accessibility-relevant) consider seeding it as the default in `theme/colorblind.go` or `theme/high_contrast.go` |
| A `theme.Range` slot | `theme/range.go` (`Range` struct + `Clone` + `MergeRange`) + matching spec wire field in `spec/theme.go` + override copy in `theme/override.go` (`copyRange`) + JSON Schema property in `schema/v1/theme.schema.json` (`range_block`) + scale-resolution call site in `encode/palette.go` (or marks/heatmap.go for sequential) + every built-in theme that should default the slot |
| A tier of the **color palette cascade** (`scale.range`, `scale.scheme`, a `theme.Range` slot, the legacy flat fields) or `scale.interpolate` | `encode/palette.go` — `ResolveCategoricalPaletteWithOpts` / `ResolveSequentialPaletteWithOpts` are the ONE cascade; the bare `ResolveCategoricalPalette` / `ResolveSequentialPalette` are back-compat wrappers that delegate. Never add a parallel resolution path. The cascade reads `ScaleOpts` (`Scheme`, `Range`, `Interpolate` — lifted by `ScaleOptsFromSpec` in `encode/scale.go`), so a new tier is a `ScaleOpts` field plus a branch in both resolvers, plus `spec/scale_axis_legend.go`, `schema/v1/scale.schema.json`, `docs/src/concepts/encoding.md` ("Choosing the colors") and `docs/src/concepts/themes.md` (scheme catalogue). Precedence, highest first: `scale.range` → `scale.scheme` → theme `Range` slot → legacy flat field → built-in default. `scale.range` is honoured on **color channels only**; on a position channel it is REJECTED (`PRISM_SPEC_044`, `validate/rules/scale_range_position.go`) because a position scale's range is the plot rect `encode/layout.go` computes — never ignore it silently. Colorspace math lives in exactly one place, `encode/interpolate.go` (`InterpolateColor` / `ResampleRamp` / `NormalizeInterpolate`, spaces `rgb` / `hsl` / `lab`; `hcl` is out of scope and schema-rejected); a continuous ramp is resampled there at encode time so the Scene IR's stops already carry the space and no renderer — Go or the vendored JS — needs colorspace logic. |
| A semantic validation rule | `validate/RULES.md` + new rule file under `validate/rules/` + register in `validate/rules/init.go` (a `validate.RegisterDefault` line — never edit an existing rule file) + new `PRISM_SPEC_NNN` row in `errors/codes.go` |
| A spec field that becomes honoured (or a new one that is not yet) | `encode/inert.go` — the single inert-field detector E7-S1 added. `markDefOwners` maps each checked `mark_def` property to the marks whose encoder reads it; `deadChannels` / `deadLegendProps` list the bindings nothing reads; `inertScale` holds the scale-knob-per-family rules. Wiring a field up means **removing or narrowing its entry**, or a `PRISM_WARN_*_INERT` keeps firing on a feature that now works — the mirror image of the silent no-op. Leaving a new field OUT of the table is silent, never a false positive, so a property is only reported once someone records where it is honoured. Plus `docs/src/concepts/spec.md` (the warnings table) and a unit case in `encode/inert_test.go`. The detector runs from `Encode` / `EncodeComposite` at the top of the tree only (`encodeLeaf` / `encodeComposite` are the composition-cell entries) — that is what keeps one reporter per spec |
| A `PRISM_*` error code (added / removed / renamed) | `errors/codes.go` (canonical `Code`, `Message`, at least one fixup template or `SeeAlso`) + reachable via `prism errors lookup` |
| A renderer backend (SVG / Canvas) | `docs/src/concepts/themes.md` (rendering notes if visual) + `render/<backend>/` + dispatch in `render/render.go` |
| Anything reachable from `cmd/prismwasm/main.go` (WASM entry) | `docs/src/concepts/browser.md` + size-budget gate `internal/gates/wasm_tinygo_size_test.go` + `cmd/prismwasm/wasm_smoke_test.go` if behaviour changes |
| A new package import in the WASM entry, OR a new file under a `!js`-gated subtree (`rpc/`, `mcp/`, `mcpserve/`, `cmd/prism/cmd_serve.go`, `cmd_mcp.go`, `cmd_static_bundle.go`, `cmd_init.go`) | Re-run `make build-wasm-tinygo` locally; CI gates verify (a) the WASM entry still compiles under TinyGo and (b) the gzipped binary is under `PRISM_WASM_TINYGO_MAX_BYTES` |
| A CLI leaf (added / removed / flag added) | `cmd/prism/cmd_<name>.go` + `docs/src/getting-started.md` if user-visible + smoke test in `cmd/prism/*_smoke_test.go` |
| The schema bundle (`schema/v1/`) | `schema/embed.go` (the `//go:embed` directives) + bump bundle version if breaking + `docs/src/concepts/spec.md` (`$schema` reference) |
| A built-in dataset registry shape | `resolve/registry_dataset.go` + `docs/src/concepts/multi-source.md` + `PRISM_DATASETS` env var documentation below |
| A Twirp RPC method | `rpc/service.proto` → regenerate via `make proto` → `rpc/server.go` + `cmd/prism/cmd_serve.go` HTTP shim + smoke test under `cmd/prism/twirp_roundtrip_test.go` |
| An MCP tool | `mcp/server.go` (typed I/O structs + `func(ctx, *rpc.PrismServer, In) (Out, error)` handler) + `mcp/catalog.go` (register a `ToolDescriptor` in `Tools(cfg)` — name, description, `reflectSchema[In]`/`reflectSchema[Out]`, and an `Invoke` closure bridging to the handler; schemas are reflected from struct tags via jsonschema-go, never hand-written) + `mcp/gosdk/gosdk.go` (the go-sdk adapter mounts the catalog — usually no change unless the transport binding changes) + `examples/` if the tool reads the embedded spec corpus + `docs/src/cookbook/mcp-agent-integration.md`. Keep typed output flat (jsonschema-go panics on recursive types — type nested/recursive fields as `any`). The `mcp/` core must stay SDK-free (`internal/gates/mcp_firewall_test.go`) |
| The in-process MCP runner (`mcpserve/`) — `Options` fields, `Serve` / `ServeStdio` / `ServeTransport` signatures, or the server identity it advertises | `mcpserve/mcpserve.go` (keep `//go:build !js`; mount only via `gosdk.Register`, never a second registration path) + `docs/src/cookbook/mcp-agent-integration.md` (the mounting-paths list + the `mcpserve` section) + `cmd/prism/cmd_mcp.go`, which dogfoods `ServeStdio` and must keep working (`cmd/prism/geo_serve_mcp_smoke_test.go`). A new `Options` field is CLI-reachable only once `mcpOptions` in `cmd_mcp.go` fills it from a flag — otherwise the behaviour it controls silently does not exist for `prism mcp`. `cmd/prism/cmd_mcp_test.go` enforces that: it drives every `prism mcp` flag at a non-zero value through the real command and reflects over the resulting `Options`, failing on any exported field left at its zero value (add the new flag to its args list, or record a reason in `mcpOptionsAllowlist`) |
| An environment variable | This file ("Build / Env" section) + `internal/limits/limits.go` (defaults + parser) if numeric |
| An optimizer pass | `plan/passes/register.go` + `docs/src/concepts/spec.md` (Plan section) + entry in `plan/optimize.go` |
| A `prism init` template (`.prism/`) | `cmd/prism/templates/` + smoke test in `cmd/prism/init_test.go` + `docs/src/getting-started.md` editor-setup paragraph |
| `prism static-bundle` output shape | `cmd/prism/cmd_static_bundle.go` + `static/staticfs.go` + smoke test |
| A playground example (`docs/src/playground/examples/`) | `docs/src/playground/examples/manifest.json` (id + title + file) + the new `<id>.json` spec file. Specs must use inline `data.values` or `datasets.*.values` — the playground has no `.pulse` fetch path |
| A projection in `encode/projection/` or a feature in `geodata/` | `docs/src/concepts/geo.md` + `schema/v1/projection.schema.json` (the `type` enum) + manifest regeneration via `make geodata` when admin-level data changes |
| A `data` block variant (`name`, `ref`, `values`, `feature_collection`) | `spec/data.go` (struct field + UnmarshalJSON discriminator) + `schema/v1/data.schema.json` (oneOf entry) + `plan/build/build.go` registerDataset case + `docs/src/concepts/geo.md` for geo-specific variants + `docs/src/concepts/multi-source.md` for runtime-ref variants (caller-supplied `DataResolver` in `resolve/data_resolver.go`). The external `source` (`.pulse`) variant was removed in E4-S3 — Prism never reads `.pulse`; a `source` key is rejected at decode with `PRISM_SPEC_039`. `spec.Data.Source` survives only as an internal (`json:"-"`) binding target populated by the server-side `DatasetRegistry` (a `name` reference resolving through `PRISM_DATASETS`/`--datasets-config`) and by test harnesses. |
| An easing in `spec.AnimationEasings` or any field in `spec.Animation` | `spec/animation.go` (const list + struct) + `schema/v1/animation.schema.json` (enum / properties) + `docs/src/concepts/spec.md` (Animation table) + `static/vendor/prism/prism-animator.mjs` (`EASINGS` table + `_normaliseAnimation`) + `internal/devtools/cross-impl-runner/animator-tween.mjs` if behaviour changes |
| A numeric or color SVG attr the animator should tween | `static/vendor/prism/prism-animator.mjs` (`NUMERIC_ATTRS` or `COLOR_ATTRS`) + `docs/src/concepts/browser.md` (Animation section) + `internal/devtools/cross-impl-runner/animator-tween.mjs` fixture coverage |
| A gallery fixture under `docs/src/gallery/animation/` | new `<name>.prism.json` + golden `<name>.svg` (regen via `UPDATE_GOLDENS=1 go test ./cmd/prism/ -run TestPrismGalleryFixtures`) + entry in `docs/src/gallery/index.md` Animation section + `<prism-chart>` card in `docs/src/gallery/index.html`. `.scene.json` is built by `make docs-scenes` and gitignored |
| A condition on an encoded channel | `spec/condition.go` (a condition `test` is a structured `spec.Predicate` — the same grammar `filter` uses, from `spec/predicate.go`, not an expression string) + `spec/encoding.go` (`ChannelCommon.Condition`) + `schema/v1/encoding.schema.json` (`condition_test` $def referencing the `filter_predicate` grammar + `condition` ref on `channel_base`/`position_channel`/`mark_channel`) + `validate/rules/condition_selection_ref.go`/`condition_test_parses.go` (reuses the shared `checkPredicate` helper under `PRISM_SPEC_026`)/`condition_value_or_binding.go` + `errors/codes.go` (`PRISM_SPEC_025/026/027`) + `encode/scene/condition.go` (`ConditionalAttr`) + `encode/encode_condition.go` (evaluates the predicate row-by-row) + `static/vendor/prism/prism-selection.mjs` (`applyConditions`) + `docs/src/concepts/encoding.md` (Conditions section) |
| The `text` encoding channel (label content, `format`, `aggregate`) | `spec/encoding.go` (`TextChannel`) + `schema/v1/encoding.schema.json` (`text_channel` $def) + `marks.Inputs.Text` in `encode/marks/marks.go` + a `Text:` binding at **every** `marks.Inputs` construction site (`encode/encode.go`, `encode/encode_composite.go`) + the content resolver `textContents` in `encode/marks/text.go` + `injectEncodingAggregate` in `plan/build/build.go` (so `text.aggregate` injects the synthetic GroupAggregateNode and a non-aggregated `text` field joins the group-by) + `validate/rules/format_string_valid.go` (`PRISM_SPEC_011` covers `text.format`) + `docs/src/concepts/marks.md` (Text section) + `docs/src/concepts/encoding.md` (Text channel section). The no-text-channel fallback (y-field value verbatim, x when y is unbound) is golden-pinned — changing it moves every committed text-mark SVG. `textContents` has a second family of consumers since E4-S5 — the graph marks' node labels (row below) — so a change to its resolution order moves tree / dendrogram / network output too. |
| Per-column null support for a new aggregate, scale, mark, or transform | the new code + `table.Column.IsNull` / `NullCount` consultation + `PRISM_WARN_NULL_*` emission when applicable (`errors/codes.go`) + `docs/src/concepts/multi-source.md` null table |
| Which encoding channels are **scale-bound** (i.e. hand raw field values to `Scale.Apply`), or the encode-time null policy | `encode/encode.go` — `usesCartesianScales` (the one predicate both the flat and layer-composite encoders consult) + `scaleBoundChannels` (the channel → field list) + `encode/marks/null_filter.go` (`DropNullRows`, the single implementation; `SkipNullRows` is its row test) + `docs/src/concepts/multi-source.md` (null table + "Nulls at encode time"). Do NOT re-derive the filter inside a mark encoder — filtering happens once on the table, before scale resolution, so scale domains, colour categories, `group.go`'s partitioner, tooltips, datum back-references, category styles and conditions all see the same rows. A null-free table is returned untouched, which is what keeps every committed golden byte-identical. |
| Inline type inference — how `data.values` rows become a typed `table.Table` | `table/inline.go` (`inferInlineColumns` resolves column order + kind) + the validate-side twin in `internal/validatorutil/lookup.go` (`registerStatic`, which must agree with it so `SchemaShim` field types match the executed table) + `docs/src/concepts/multi-source.md` null table |
| A `mark_def` style property (`spec/mark.go` `MarkDef` field) | `spec/mark.go` (struct field) + `schema/v1/mark.schema.json` (`mark_def` property) + the consumer: a *style* property lands on `scene.Style` via `encode.applyMarkDef` (`encode/encode.go`) and is emitted by `render/svg/style.go`; a *geometric* property lands on the geom in the owning encoder under `encode/marks/` and is consumed by `render/svg/marks.go`. Any new `scene.Style` / geom field is a Scene IR addition — mirror it in `static/vendor/prism/prism-animator.mjs` (`NUMERIC_ATTRS`) when it is a tweenable numeric SVG attr + `docs/src/concepts/marks.md` (Style properties section). If `theme.MarkStyle` carries a same-named token, wire it in `encode.applyThemeMarkStyle` too — that function runs **before** `applyMarkDef`, which is what makes spec-over-theme precedence hold per field (documented in `docs/src/concepts/themes.md`, Mark style precedence). `theme.MarkStyle` and `spec.MarkDef` have identically named fields (`FillOpacity`, `FontWeight`, `FontStyle`, `StrokeWidth`, …) — check the receiver's type, never just the selector name. |
| A line / area interpolation (`scene.CurveType`) | `encode/scene/geom.go` (the `CurveType` const block + `LineGeom`/`AreaGeom` fields) + `encode/marks/curve.go` (`curveFor` — the spec `interpolate`/`tension` → scene mapping) + `render/svg/curve.go` (the path emitter; every control point through `render.FormatFloat`) + `schema/v1/mark.schema.json` (`interpolate` enum) + `docs/src/concepts/marks.md` (Interpolation section) + a gallery fixture under `docs/src/gallery/basic-marks/`. `CurveLinear` MUST keep rendering as `<polyline>` — only non-linear curves become `<path>`, which is what pins every committed linear golden and `testdata/cross_impl/` fixture. Run `PRISM_CROSS_IMPL=1` and `PRISM_CROSS_IMPL_TINYGO=1` for any change to the curve maths. |
| **Node labels on a graph mark** (`tree`, `dendrogram`, `network`) — content, placement, or the room they claim | `encode/marks/node_label.go` — the ONE place the graph family resolves label content, estimates label width and insets the plot rect (`nodeLabelsBound` / `nodeLabelsByID` / `nodeLabelFor` / `maxNodeLabelWidth` / `insetForLabels`). Content comes from `textContents` (`encode/marks/text.go`), the text mark's own resolver — never a second content path, so `field` / `value` / `format` behave identically everywhere. Consumers: `encode/marks/tree.go` (also serves `dendrogram` via `encode/marks/dendrogram.go`) and `encode/marks/network.go`. Two invariants: (a) labelling is **opt-in on the `text` channel** — `in.Text == nil` must emit no label geometry and leave the plot rect un-inset, which is what keeps an unlabelled tree / network byte-identical; (b) a row's label keys to that row's **`target`** value (the node identity), first-row-wins, and a node that never appears as a target falls back to its id. Placement is `orient`-aware for the tree family only (internal nodes label away from their subtree — above / left; leaves below / right); a `network` has no growth direction so every label hangs below its node. Width is estimated from `scene.LabelCharWidth`, the shared 6 px/char constant `encode/axis_build.go`'s `axisLabelCharWidth` also aliases — there is exactly one such estimate in the tree, do not add a second. Labels are never dropped or truncated: hiding one loses a node's identity. Style comes from `marks.Inputs.LabelStyle` (the theme's `text` default), NOT `Inputs.Style` (which resolves the outer mark type). `scene.TextGeom.Baseline` is left unset on purpose — neither `render/svg` nor the vendored JS honours it, so vertical offsets are baked into `Y`. Docs: `docs/src/concepts/marks.md` (Node labels) + `docs/src/concepts/encoding.md` (Text channel › On a graph mark). |
| The `progress` mark — its geometry, its `total` / `thickness` mark-def fields, or the **track** it draws | `encode/marks/progress.go` (the encoder; orientation comes from `MarkOrientation` + the four helpers in `orient.go`, NOT a per-mark orientation field — `bullet`'s is the one exception and must not be copied) + `encode/progress.go` (`progressMeasureExtras` widens the measure domain with `total` so the track cannot overflow the plot; `progressMeasureIsX` answers the orientation question from the spec alone, early enough to feed that domain; `progressTrackStyle` derives the track's paint from the theme) + `marks.Inputs.TrackStyle` (the **track seam** — the track is a distinct scene mark, `progress-track-N`, with its own Style, never a backdrop fused into the bar's rect) + both `marks.Inputs` construction sites (`encode/encode.go`, `encode/encode_composite.go`) + `spec/mark.go` (`Total` / `Thickness` / `ProgressThicknessDefault`) + `schema/v1/mark.schema.json` + `validate/rules/progress_structure.go` (`PRISM_SPEC_061`) + the `progress` entries in `validate/rules/channel_for_mark.go`, `mark_orient.go` (`orientAwareMarks`) and `theme_mark_type_known.go` + `docs/src/concepts/marks.md` (Progress section) + the `docs/src/gallery/progress/` fixtures. The track colour is theme-derived (the grid colour today), never a constant — do not repeat `bulletBandShade`'s hardcoded greys. |
| A new mark family that needs a layout algorithm | `encode/marks/<family>.go` + `encode/marks/layout/<algo>.go` + `spec/mark.go` (`MarkDef` fields) + `schema/v1/mark.schema.json` (`mark_type` enum + `mark_def` properties) + `validate/rules/channel_for_mark.go` allowlist + dedicated validate rule under `validate/rules/<family>_*.go` if structural invariants apply + `errors/codes.go` + `docs/src/concepts/marks.md` section + 2-4 gallery fixtures under `docs/src/gallery/<family>/` |

If you find yourself wanting to defer the doc update to "a follow-up PR," stop. The follow-up will not happen, the next Claude Code session will read stale guidance and produce wrong code. Update in the same PR or do not merge.

## Architecture

```
prism/
├── prism.go                # Root Go API: Compile, RenderPlan, CompiledPlan
├── patch.go scene.go       # RFC 6902 patch (Patch/PatchOp/ApplyPatch/DiffSpecs) + Scene wrapper
├── selection/              # Structured selection event (Event, SelectedMark, DataRowRef)
├── cmd/prism/              # Host CLI binary (gated `//go:build !js` where needed)
│   ├── main.go             # urfave/cli/v3 wiring
│   ├── cmd_*.go            # one file per CLI leaf
│   ├── templates/          # `prism init` payload (schemas + examples + editor configs)
│   └── *_smoke_test.go     # per-command end-to-end checks
├── cmd/prismwasm/          # WASM entry — `//go:build js && wasm`
│   ├── main.go             # exports validate/plan/execute/compile/render/errorsLookup/schemaBundle/version/setDataResolver/applyPatch/diffSpecs on globalThis.prism via syscall/js
│   └── wasm_smoke_test.go  # Node + wasm_exec runner against committed fixtures
├── spec/                   # Spec types + decoders (Mark, Encoding, Transform, Selection, Composition)
├── validate/               # Shape + semantic validation (no row I/O)
│   ├── shape.go            # Schema-aware structural checks
│   ├── semantic.go         # Rule registry runner
│   ├── lookup.go           # Field/dataset lookup (native schema shim + static inline)
│   ├── RULES.md            # PRISM_SPEC_NNN rule catalogue
│   └── rules/              # One file per semantic rule
├── plan/                   # DAG builder + sequential/parallel executor
│   ├── dag.go              # Node graph + topological sort
│   ├── builder.go          # Spec → DAG
│   ├── execute.go          # Bounded worker pool, partial failure
│   ├── cache.go cache_lru.go # Table cache (LRU)
│   ├── optimize.go passes/ # DedupSources, FilterPushdown, ProjectionPruning, AggregateFusion, SampleInjection
│   ├── render.go           # Plan diagnostics (text / dot / json)
│   └── nodes/              # Source, Inline, Filter, Bin, Calculate, GroupAggregate, Join, Limit, Pivot, Project, Sample, Sort, Stack, TimeUnit, Union, Unpivot, Window, Crosstab, Regression
├── compile/                # Transform → in-memory execution over table.Table
│   ├── aggregates.go       # Friendly aggregate-alias catalogue (names only; all client-side)
│   └── inmem/              # In-memory backend: filter/calculate/aggregate, hash join, crosstab pivot, OLS regression, stack accumulation
├── encode/                 # Scene IR + scales + axis + legend + palette
│   ├── encode.go           # Main spec → scene encoder
│   ├── encode_composite.go # layer / concat / facet / repeat
│   ├── encode_facet.go encode_repeat.go encode_selection*.go
│   ├── stack.go            # Rebinds a stacked position channel onto the StackNode's bounds columns
│   ├── layout.go scale.go palette.go ticks*.go axis_build.go axis_ticks.go legend_build.go
│   ├── selection_build.go  # Selection materialisation
│   ├── marks/              # Per-mark encoders (bar, line, area, point, rule, text, tick, rect, arc, pie, donut, histogram, heatmap, boxplot, violin, sankey, funnel, sparkline, sparkbar, winloss, sparkarea, bullet, progress, image, path, geoshape, geopoint)
│   │   ├── group.go        # The single row partitioner: groupChannels + groupRows over the (color, detail…) tuple
│   │   └── null_filter.go  # The single null policy: DropNullRows drops rows null in a scale-bound channel
│   ├── scale/              # linear, log, pow, sqrt, time, band, point, ordinal
│   ├── projection/         # Geographic projections (mercator, equirect, naturalearth, albers_usa, orthographic)
│   ├── scene/              # Scene IR types (Mark, Geom, Axis, Legend, Theme, Selection, Annotation, …)
│   ├── resolve/            # Cross-layer domain + scheme resolution
│   └── format/             # d3-format subset
├── render/                 # Bytes
│   ├── render.go           # Renderer interface + RenderOpts (no factory — each caller switches on format)
│   ├── precision.go        # Pinned 3-decimal coordinate quantisation
│   ├── svg/                # Go SVG renderer (canonical)
│   ├── html/               # Go HTML renderer — wraps svg's own emitters in an HTML document shell
│   └── canvas/             # Vendored ESM web component bridge (see `static/`)
├── resolve/                # Data source resolution
│   ├── default.go          # Inline-rows resolver (InlineResolver seam; no `.pulse` I/O)
│   ├── registry_dataset.go # `datasets` block + `PRISM_DATASETS` env
│   ├── data_resolver.go    # DataResolver interface + Dataset (runtime `data: {ref}` variant)
│   └── resolver.go         # Resolver interface
├── theme/                  # Theme registry + loader
│   ├── light.go dark.go print.go
│   ├── css.go              # CSS variable manifest
│   └── loader.go override.go
├── geodata/                # Manifest (countries + admin-1 IDs / bbox) + tier bundles (TopoJSON-lite)
├── schema/v1/              # JSON Schema bundle (`urn:prism:schema:v1:spec`)
├── errors/                 # PRISM_* code catalogue + AppError envelope
├── rpc/                    # Twirp service (proto + generated + server)
├── mcp/                    # SDK-free MCP core: typed I/O + handlers + ToolDescriptor catalog (`Tools(cfg)`); imports NO MCP SDK
│   └── gosdk/              # go-sdk adapter (sanctioned SDK importer); `Register(server, facade, cfg)` mounts the catalog + example corpus
├── mcpserve/               # In-process MCP runner (sanctioned SDK importer): `Options` + `ServeTransport(ctx, facade, opts, t)` + `Serve(ctx, facade, opts, in, out)` + `ServeStdio(facade, opts)`
├── examples/               # Stdlib-pure `//go:embed`'d spec corpus (List/Get/Search/Invalid/All); shared by `prism examples` + `examples_search`
├── static/                 # Vendored ESM bundle for `prism static-bundle`
├── table/                  # In-memory tabular intermediate
├── testdata/               # Golden fixtures + cross-impl artifacts
├── docs/                   # mdBook source (GitHub Pages)
├── internal/
│   ├── devtools/           # Cross-impl runner (Go vs JS scene IR)
│   ├── gates/              # Repo-wide structural / hygiene tests
│   ├── limits/             # Env-driven memory ceilings (PRISM_*_MAX_*)
│   ├── observability/      # Logging / metrics shims
│   ├── tools/              # One-off codegen / maintenance
│   └── validatorutil/      # Shared validate helpers
```

`cmd/prism` commands map 1:1 to public CLI leaves: `version`, `validate`, `errors lookup`, `plan`, `execute`, `plot`, `scene`, `serve`, `mcp`, `examples`, `schema`, `init`, `static-bundle`. Internal commands (none today) live behind hidden flags.

`mcp/` is the SDK-free agent core: it defines the typed tool I/O, the typed handlers, and the `ToolDescriptor` catalog (`Tools(cfg)`), reflecting JSON Schemas from struct tags via jsonschema-go. The invariant that matters — and the only one gated — is that **this core imports no MCP SDK**, so a consumer can mount the catalog without transitively acquiring one; `internal/gates/mcp_firewall_test.go` scopes exactly to `mcp/`. Two packages outside that core are sanctioned SDK importers: `mcp/gosdk/`, whose `Register(server, facade, cfg)` mounts the catalog and the embedded `examples/` corpus onto a **caller-supplied** go-sdk server (it never constructs or runs one), and the root `mcpserve/`, the in-process runner that does construct and run one — `ServeTransport(ctx, facade, opts, t)` over any caller-supplied `mcpsdk.Transport` (the base entry point; the caller owns the transport's lifetime), `Serve(ctx, facade, opts, in, out)` over any io pair (wraps the streams in non-closing adapters and delegates to `ServeTransport`), `ServeStdio(facade, opts)` over the process's stdin/stdout — by calling `gosdk.Register` internally, so `Register` stays the single mounting path. `cmd/prism/cmd_mcp.go` dogfoods `mcpserve.ServeStdio` (threading `versionString` via `mcpserve.Options`). `rpc/` exposes the same facade over Twirp HTTP behind `prism serve`.

Documentation lives in `docs/` (mdBook, published to <https://frankbardon.github.io/prism/>). The schema bundle in `schema/v1/` is the machine-readable contract loaded by editors (via `prism init`) and by `validate/` at runtime.

## Code Conventions

### Naming

- All identifiers, comments, docs are Prism-native. Module path: `github.com/frankbardon/prism`.
- `PRISM_*` is reserved for error codes and environment variables. Use `PRISM_<DOMAIN>_NNN` (`PRISM_SPEC_001`) for numbered codes and `PRISM_<DOMAIN>_<DESCRIPTOR>` (`PRISM_RENDER_FORMAT_UNAVAILABLE`, `PRISM_JOIN_MAX_ROWS`) for descriptor-style codes. Warnings use the `PRISM_WARN_*` prefix.
- Spec field keys are snake_case (`stroke_width`, `corner_radius`, `font_size`). Single-word Vega-Lite vocabulary (`mark`, `encoding`, `transform`, `layer`, `facet`) stays as-is. Channel names (`x`, `y`, `x2`, `y2`, `color`, `size`, `shape`, `opacity`, `text`, `tooltip`, `href`, `theta`, `radius`, `detail`, `order`) stay verbatim from Vega-Lite.
- Aggregate aliases mirror Vega-Lite and are all computed client-side over the materialized `table.Table` (`compile/inmem/group_aggregate.go`; `compile/aggregates.go` is a name catalogue only — no backend op constant): `count`, `sum`, `mean`, `median`, `min`, `max`, `stdev`, `variance`, `q1`, `q3`, `ci0`, `ci1`. Prism adds `distinct`, `mode`, `frequency`, and the distribution-shape scalars `range`, `skewness`, `kurtosis`, `null_count` (`distinct`, `mode`, `frequency`, `null_count` are universal — any field type; the distribution-shape scalars are quantitative/temporal only). Cohort-analytics extensions are `wmean`, `ratio`, `lift`, `share`. `frequency` is the SCALAR modal count (occurrences of the most frequent value, the multiplicity companion to `mode`). A `zscore` aggregate is intentionally NOT offered — a per-group mean z-score is always 0.
- Mark names are bare nouns: `bar`, `line`, `area`, `point`, `rule`, `text`, `tick`, `rect`, `arc`, `pie`, `donut`, `histogram`, `heatmap`, `boxplot`, `violin`, `sankey`, `funnel`, `sparkline`, `sparkbar`, `winloss`, `sparkarea`, `bullet`, `progress`, `image`, `path`.

### Error handling

Six error domains live under `errors/codes.go` (`Codes` map): `SPEC`, `RESOLVE`, `PLAN`, `COMPILE`, `ENCODE`, `RENDER`, plus per-feature descriptor codes (`JOIN`, `SERVE`, …) and the `PRISM_WARN_*` warning family. Every code carries:

- `Code` — canonical `PRISM_*` identifier.
- `Message` — terse one-liner.
- At least one of: `Fixups` template list OR a non-empty `SeeAlso` cross-reference.

`errors.New(code, message, details)` builds the `AppError` envelope. CLI surfaces JSON envelopes via `--json`; human output is `<CODE>: <message>` plus rendered fixups. Reactive lookup is `prism errors lookup CODE` (CLI) and the equivalent MCP tool.

Validation rules live one-per-file under `validate/rules/`. Each rule implements `SemanticRule` from `validate/semantic.go`:
**Inert-field warnings (E7-S1).** A spec key that decodes, validates and then reaches no consumer reports itself rather than being discarded: `encode.InertFieldWarnings` walks the spec once, from the top of the composition tree, and emits `PRISM_WARN_MARK_DEF_INERT` / `PRISM_WARN_CHANNEL_INERT` / `PRISM_WARN_SCALE_FIELD_INERT` / `PRISM_WARN_LEGEND_FIELD_INERT` / `PRISM_WARN_LEGEND_NOT_BUILT` / `PRISM_WARN_FACET_CHILD_SKIPPED` onto `SceneDoc.Warnings`. They are warnings, never errors — the chart still renders, and `prism plot` / `prism scene` print them to stderr. Nothing rejected at validate is reported here (a rejection is already visible), and nothing honoured may be: a false positive trains authors to ignore warnings. See the Update Demand row for what to edit when a field stops being inert.

Validation rules live one-per-file under `validate/rules/`. Each rule implements `Rule` from `validate/semantic.go`:

```go
type SemanticRule interface {
    Code() string                                              // canonical PRISM_SPEC_NNN
    Check(s *spec.Spec, schemas SchemaLookup) []*errors.AppError // emit zero or more errors
}
```

Rules register through `validate/rules/init.go` (a `validate.RegisterDefault` line, loaded via `init()`). Add a new rule by dropping a file and registering it — do not modify existing rule files.

### Output Format Contract

- **No `fmt.Sprintf`-built JSON.** All structured output goes through `encoding/json`. CLI envelopes are built explicitly so missing fields fail at compile time.
- **Stable Scene IR.** `encode/scene/` types serialise to a stable JSON shape consumed by the JS-side renderer. Field additions are additive; renames or removals require a version bump and a JS-side migration.
- **Pinned coordinate precision.** The SVG renderer rounds coordinates via `render.precision.go` to 3 decimal places. Adding a new geometric primitive MUST route through the precision helper so cross-impl goldens stay stable.
- **Golden parity.** SVG goldens live under `render/svg/testdata/` and `cmd/prism/templates/` smoke fixtures. JS-side comparison fixtures live under `testdata/cross_impl/` — `scene.json` + `go.svg` are committed; `js.svg` + `diff.txt` regenerate per run (gitignored).

### Plan + Execute

`plan.Build(spec, registry) (*Plan, error)` constructs the DAG without executing. `plan.Execute(ctx, p, opts)` runs it. Topological order with bounded worker fan-out per `ExecOpts.Workers` (0 ⇒ `PRISM_QUERY_WORKERS` env ⇒ `runtime.NumCPU()`; 1 ⇒ serial). Partial-failure policy controlled by `ExecOpts.FailFast` (defaults true). Optimizer passes run between Build and Execute in this order: `DedupSources`, `FilterPushdown`, `ProjectionPruning`, `AggregateFusion`, `SampleInjection`. All transforms execute over the in-memory backend against the materialised `table.Table`. Add new passes via `plan/passes/register.go`. Two nodes are injected by the builder rather than written by the author: the synthetic `GroupAggregateNode` an `aggregate` on any channel implies, and — on top of it — the `StackNode` `spec.ResolveStack` selects (see the Stacking row above). The stack node is always the last node before the sink, because its derived columns are what the encoder draws from.

### Scale domains

`encode/scale.go` owns domain resolution. Every `resolve*` arm takes a `ScaleOpts` (built from the spec `scale` block by `ScaleOptsFromSpec`) and applies a fixed precedence: an explicit `scale.domain` **wins outright** (neither `zero` nor `nice` runs on top of it); otherwise the data extent is widened by `zero` and then rounded by `nice`.

Defaults mirror Vega-Lite: `zero` is on for `linear` / `pow` / `sqrt`, `nice` is on for `linear` / `pow` / `sqrt` / `time`. Two asymmetries are load-bearing and must survive refactors — **`log` never gets zero-forcing** (a log domain cannot contain zero; an explicit `zero: true` is ignored), and `nice` defaults **off** for `log` because its ticks are already base-aligned. `zero` does not apply to `time` either. Nice rounding uses `tickIncrement` at the axis builder's tick count (5) so the domain bounds coincide with the outermost tick; the temporal variant (`niceTimeDomain` in `encode/ticks_time.go`) rounds to the calendar boundary the tick generator would pick.

A discrete `domain` (`band` / `point` / `ordinal`) pins the leading category order and appends any unlisted data category after it — an intentional divergence from Vega-Lite, chosen so a partial domain reorders without dropping rows. `nice` is boolean-only on the wire; the schema rejects Vega-Lite's numeric tick-count form (use `axis.tick_count`). Malformed domains raise `PRISM_SPEC_042` from both `validate/rules/scale_domain.go` and the encoder.

### Domain → pixels

Where `domain` / `zero` / `nice` decide *what* the scale covers, `clamp` / `reverse` / `round` / `padding` / `padding_inner` / `padding_outer` / `align` decide how it lands on pixels. All seven ride the same `ScaleOpts` and the same four threading sites.

- **`clamp` / `round` on a continuous scale** live on `scale.ContinuousOutput` (`encode/scale/output.go`), embedded by `LinearScale` / `LogScale` / `PowScale` and inherited by `SqrtScale` / `TimeScale` through the scale they wrap. `interpolate(t, …)` is the one place the policy applies — never re-implement it per family. Clamping pins the normalised position into [0,1], which is the same thing as pinning the input to the domain edge; a non-positive value on a log scale still raises `PRISM_SPEC_010`, because it has no image at all.
- **Band / point geometry** is `layout()` in `encode/scale/band.go` and `point.go` — a single d3-scale-shaped formula returning `(step, offset, width)`. The span is **signed**: a y range arrives as `(bottom, top)`, so step / offset / `BandWidth()` all come back negative and the mark encoders normalise through `rectAxisExtent` (`encode/marks/orient.go` `CategorySlots`, plus the inline normaliser in `encode/marks/heatmap.go`). Do not "fix" the sign inside the scale.
- **The band defaults are load-bearing.** `padding_inner` 0.1 + `padding_outer` 0.05 + `align` 0.5 are Vega-Lite's *and* reproduce the pre-split `step = span/n, half-an-inner-gap at each end` layout byte-for-byte. Changing any of the three moves every bar, tick, heatmap and boxplot golden. `padding` is the shorthand (both band paddings, or a point scale's only outer padding); explicit `padding_inner` / `padding_outer` outrank it. A point scale has no inner padding, so `padding_inner` never reaches it.
- **`reverse` is applied per family**, and is defined relative to the channel's default direction (y already runs bottom-to-top, so `reverse` puts the domain minimum at the *top*). Continuous families flip the pixel range via `ScaleOpts.pixelRange`; discrete families reverse the **slot assignment** inside the scale, which is what keeps the signed step and the negative `BandWidth()` intact. Flipping a band's range instead would negate every horizontal bar and heatmap row. Axes, ticks and gridlines need no separate handling — they place through `Scale.Apply`.
- **Every scale is built through one constructor** — `NewBandScale` / `NewPointScale` / `newLinearScale` / `newOrdinalScale` in `encode/scale.go` — so the flat encoder and `scaleFromUnified` (`encode/encode_composite.go`) cannot drift apart on a knob.
- Out-of-range paddings / `align` are rejected at validate with `PRISM_SPEC_049` (`validate/rules/scale_band_geometry.go`); the encoder pins them defensively for callers that skip validate. `round` is a *layout* quantisation and composes with `render/precision.go`'s 3-decimal *serialisation* pinning — they are different mechanisms, do not merge them.

### Composition

`encode/encode_composite.go` handles `layer`, `concat`, `hconcat`, `vconcat`, `facet`, `repeat`. Cross-layer scale resolution defaults to **shared** for matching channel + field pairs; opt-out via `resolve: "independent"` per scale. `facet`/`repeat` expand into per-cell child scenes whose absolute positions land via `encode/layout.go`.

Per-channel `axis` config survives composition under **both** resolve modes. Independent axes read their own child's block via `axisOptsFor`; a shared axis folds the children's blocks in `encode/axis_shared.go` (`sharedAxisOpts` → `mergeSharedAxisSpec`) under a **first-specified-wins, per-property** rule — the first child in declaration order to specify a property wins, and a later child specifying it differently is ignored *and* reported as `PRISM_WARN_AXIS_CONFIG_CONFLICT`. A conflict is never silently resolved. The fold is reflective over `spec.Axis`, so new axis properties are covered automatically. The title falls back to the first child's field name only when no child set `axis.title`; an explicit `"title": false` still suppresses. The rule is documented for authors in `docs/src/concepts/composition.md`.

### Row ordering

`encoding.order` (E5-S4) is Vega-Lite's overloaded ordering channel, and Prism resolves all three of its senses through **one** mechanism: the row order of the table the encoder consumes. `spec.ResolveOrder` (`spec/order.go`) is the single decision point, called by both `plan/build/build.go`'s `injectEncodingOrder` and the encoder, exactly as `spec.ResolveStack` is shared by plan and encode.

Binding `order` injects a `SortNode` **after** the synthetic encoding aggregate and **before** the `StackNode`. From that one reordering:

- **stack order** falls out — `plan/nodes/stack.go` ranks segments by first appearance of the stack-by tuple;
- **draw order** falls out — every per-row mark encoder emits in table order, so the last one emitted paints on top;
- **path point sequence** needs the only encoder cooperation in the feature: `marks.Inputs.Ordered` suppresses line/area's default left-to-right x-sort so each group traces in table order.

Do not add a second reordering path at encode. A non-aggregated order field joins the synthetic aggregate's group-by (the treatment `detail` gets, so it survives to the sort below it); a field another channel already aggregates pointedly does **not** widen the group-by, so the order key reads that aggregate's output column instead.

With `order` unbound `ResolveOrder` returns nil, no node is injected, and table order is preserved exactly — that is the Vega-Lite rule and it is what keeps every pre-E5-S4 chart byte-identical. An `aggregate` on an order entry is **rejected** (`PRISM_SPEC_056`), not honoured; a malformed entry or an unknown `sort` direction is `PRISM_SPEC_055`.

Sort-direction vocabulary is owned by `spec.SortDirectionDescending` / `SortDirectionValid`: canonical `ascending` / `descending`, with `asc` / `desc` as aliases. `plan/nodes.SortKey.Descending()` delegates there and the in-memory executors call that method — never a string compare. Every schema enum naming directions must list all four; the enum disagreeing with the code is how `"descending"` shipped as a silent no-op in the `sort` and `window` transforms until E5-S4.

### Multi-source

The `datasets` block in a spec declares named cohorts. Per-layer / per-mark `data` overrides bind to a dataset by name. Hash join is a transform (`{join: {left, right, on, kind}, as}`) with kinds `inner`, `left`, `outer`, `anti`. Cardinality is bounded by `PRISM_JOIN_MAX_ROWS` (default 5,000,000); overflow returns `PRISM_JOIN_001` with the offending product in `details`.

**Encode-time null policy.** `left` / `outer` joins (and inline `null` literals) surface unmatched cells as null. Before any scale resolves, `marks.DropNullRows` removes every row carrying a null in a **scale-bound** channel — today exactly `x` / `y`, and only for marks that go through the standard cartesian scale resolution (`usesCartesianScales` in `encode/encode.go`; polar / histogram / specialty / geo marks bring their own geometry and are exempt). Dropping some rows emits `PRISM_WARN_NULL_DROPPED` with the count + offending channels; dropping *all* of them fails with `PRISM_ENCODE_NULL_ALL_ROWS` rather than rendering an empty chart. A null in a non-scale-bound channel (`color`, `tooltip`, `text`, `detail`, …) never removes a mark. Dropping the row is a chosen semantic, not an oversight: Prism does NOT gap-break the line/area path around the hole the way Vega-Lite does. Server-side registry: `resolve.DatasetRegistry` (loaded from `--datasets-config` JSON file + `PRISM_DATASETS` env, chained file → env). Browser-side: declared via `<prism-chart datasets="…">` attribute on the web component (see `static/`).

### Selections

`spec.Selection` (point + interval) compiles to `encode/scene.Selection` and is rendered as either client-resolved overlays (web component) or server-resolved derived datasets (Twirp / MCP). The two modes share the same selection grammar; mode is chosen by the renderer backend.

### Theming

Three built-in themes ship: `light` (default), `dark`, `print`. Each lives in `theme/<name>.go` and supplies a `theme.Tokens` struct (colors, fonts, sizes). The renderer materialises tokens as CSS variables in the SVG output via `theme/css.go` — downstream consumers can theme post-hoc by overriding variables. The `axis` block applies to both cartesian axes; `axis_x` / `axis_y` layer over it per property for one axis only (`theme.Theme.AxisFor` — see the per-axis row in the Update Demand), which is also how the vertical (x) and horizontal (y) grid lines are themed apart. No built-in theme sets either block: they exist so a theme author can differentiate, and their absence is what keeps output identical to a pre-`axis_x` theme. Custom themes load from `theme.json` via `theme/loader.go`; sparse spec-level overrides merge through `theme/override.go`. Adding a token requires updating every built-in theme and `theme/css.go`'s manifest emitter.

### Geographic marks

`geoshape` (choropleth polygons) and `geopoint` (lon/lat overlays) live alongside the rest of the encoders in `encode/marks/`. Spec block `projection.type` selects the lon/lat → pixel map (`mercator`, `equirectangular`, `naturalearth`, `albers_usa`, `orthographic`); per-projection params live in `encode/projection/`. Feature geometry comes from the `geodata/` package: a small manifest (~128 KB) is embedded in every build (host + WASM) for validate/plan; full tier bundles (`world-110m`, `world-50m`, `admin1-50m`) are NOT embedded — the host build loads them at runtime from a directory supplied via the `--geodata-dir` flag or the `PRISM_GEODATA` env var (`<dir>/<tier>.geo.json`), and rendering a geo mark with no directory configured hard-fails with `PRISM_GEODATA_DIR_UNSET` (a configured dir missing the requested tile fails with `PRISM_GEODATA_TIER_MISSING`). The WASM build fetches tiers from `${origin}/static/prism/geodata/` (override via `prism.geo.setBundleURL(url)` or `data-prism-geodata-url` attribute). `prism static-bundle` sources the tiers from `--geodata-dir` and emits the geodata files alongside the JS bundle so the WASM runtime works out-of-the-box. The committed tiers live in the repo at `geodata/*.geo.json` and are published for download at `https://frankbardon.github.io/prism/static/prism/geodata/<tier>.geo.json`. `make geodata` regenerates the committed artifacts from upstream Natural Earth — `make build` itself requires no network access. For the mdBook playground, `make docs-wasm-stage` copies the geodata files into `docs/src/static/prism/geodata/` (which is a symlink to `static/vendor/prism/geodata/`); the playground sets `prism.geo.setBundleURL("../static/prism/geodata/")` so the relative path works under any mdBook deployment.

## Build / Env

`make build` (default), `make build-wasm-tinygo`, `make test`, `make test-race`, `make fmt`, `make fmt-check`, `make vet`, `make lint`, `make cover`, `make clean`, `make proto`, `make docs`, `make docs-serve`, `make docs-clean`. A `.env` at repo root is auto-loaded by the Makefile.

`make build-wasm-tinygo` is the **sole** wasm build (`tinygo build -target=wasm -stack-size=$(TINYGO_STACK_SIZE) -o bin/prism.wasm ./cmd/prismwasm`, TinyGo 0.41.1+) — the standard-Go `GOOS=js GOARCH=wasm` target was retired, so `make build` needs no wasm toolchain. It emits ~7.2 MB raw / ~2.2 MB gzip. Its companion `wasm_exec.js` comes from `$(tinygo env TINYGOROOT)/targets/wasm_exec.js` and pairs only with the TinyGo binary (never mix it with a Go-toolchain loader). The filesystem seam is routed through `internal/vfs` (a host alias for `github.com/spf13/afero`, a native afero-free interface under the `wasm` build tag) so afero — and thus `net/http`, which TinyGo cannot compile for js/wasm — never enters the WASM import graph. `-stack-size` is raised from TinyGo's ~16 KB default because the JSON-Schema shape validator recurses deep enough to trap a small stack. `cmd/prismwasm/main.go` parks `main` on a package-level channel (not `select{}`, which TinyGo folds to a deadlock panic).

**Environment variables:**

- `PRISM_DATASETS` — semicolon-separated `name=ref` list registering named datasets for `data.name` lookup (a `{"data": {"name": "…"}}` reference resolves through this registry to a source-bound leaf). `ref` is an opaque resolver identifier. Layered behind `--datasets-config` JSON file (file wins). Defined in `resolve/registry_dataset.go` (`EnvDatasetVar`).
- `PRISM_GEODATA` — single directory path holding the map tier bundles (`<dir>/<tier>.geo.json` for `world-110m`, `world-50m`, `admin1-50m`) consumed at render time by `plot`, `scene`, `serve`, `mcp`, and `static-bundle` (the `--geodata-dir` flag is the per-command equivalent; flag wins). Host tiers are no longer embedded, so this (or the flag) must be set for any `geoshape`/`geopoint` mark — unset + a geo mark hard-fails with `PRISM_GEODATA_DIR_UNSET`; a configured dir missing the requested tier fails with `PRISM_GEODATA_TIER_MISSING`. This is a path, NOT numeric, so it does NOT live in `internal/limits/limits.go`; the flag is wired in `cmd/prism/geodata_dir.go` and consumed by the host loader in `geodata/`. The no-execute leaves (`validate`, `plan`) and the data-only `execute` leaf use only the embedded manifest and ignore it.
- `PRISM_TABLE_MAX_ROWS` — cap on any single materialised `table.Table`. Default 50,000,000. Defined in `internal/limits/limits.go`.
- `PRISM_JOIN_MAX_ROWS` — cap on left × right product for the hash-join node. Default 5,000,000. Overflow → `PRISM_JOIN_001`.
- `PRISM_RENDER_MAX_MARKS` — cap on the number of marks the renderer emits before auto-`Sample` injection by the `SampleInjection` optimizer pass. Default 100,000.
- `PRISM_QUERY_WORKERS` — bounded executor worker count for `plan.Execute`. 0 (or unset) ⇒ `runtime.NumCPU()`. 1 ⇒ serial. Positive integers cap the fan-out.
- `PRISM_TABLE_CACHE_SIZE` — LRU capacity for the plan-level table cache. Default 256 entries.
- `PRISM_CROSS_IMPL` — set to `1` to opt into the cross-implementation parity tests under `internal/devtools/`. The harness compares host-native SVG vs TinyGo-via-wasm SVG (TinyGo is the sole wasm build). Needs `node` on `PATH`.
- `PRISM_CROSS_IMPL_REGEN` — set to `1` to regenerate the WASM-side scene fixtures during a cross-impl run.
- `PRISM_CROSS_IMPL_TINYGO` — set to `1` to opt into the TinyGo↔host WASM float-parity harness (`internal/devtools/tinygo_parity_test.go`): builds `cmd/prismwasm` with TinyGo, renders the fixture corpus under Node, and diffs SVG byte-for-byte against the host-Go goldens; also drives `render.FormatFloat` over an edge-case corpus in host / TinyGo-wasm and asserts they agree. Guards the top TinyGo-migration risk (`strconv`/`FormatFloat` drift through `render/precision.go`). Needs both `node` and `tinygo` on `PATH`.
- `PRISM_WASM_TINYGO_MAX_BYTES` — gzipped size ceiling for the **TinyGo**-built `bin/prism.wasm` (`make build-wasm-tinygo`, the sole browser artifact), enforced by `internal/gates/wasm_tinygo_size_test.go`. Default 4,194,304 (4 MB); soft warning at 3 MB (measured 2,547,567 B gzipped). CI pins TinyGo 0.41.1 and runs this gate as a hard requirement; locally the gate builds a fresh TinyGo module into a temp dir and **skips cleanly when `tinygo` is not on `PATH`**. Defined in `internal/limits/limits.go`.
- `PRISM_WASM_TINYGO_RAW_MAX_BYTES` — uncompressed size ceiling for the TinyGo-built `bin/prism.wasm`, enforced by the same TinyGo gate. Default 12,582,912 (12 MB); soft warning at 10 MB (measured 8,022,421 B raw). Defined in `internal/limits/limits.go`.

Numeric env vars parse loudly: a non-empty value that fails to parse, or that resolves to non-positive, is rejected by the lookup helpers in `internal/limits/limits.go` (returns default + `ok=false`). Callers may surface a config error or silently fall back via the `Must*` helpers.

Hermetic testing: `afero.NewMemMapFs()` is the default for tests under `validate/`, `resolve/`, `plan/`, `compile/`. No disk I/O in unit tests outside the goldens path.

## Spec Format ($schema)

```json
{
  "$schema": "urn:prism:schema:v1:spec",
  "data":    {"values": [{"Origin": "USA", "Horsepower": 130}, {"Origin": "Europe", "Horsepower": 90}]},
  "mark":    {"type": "bar"},
  "encoding": {
    "x": {"field": "Origin", "type": "nominal"},
    "y": {"aggregate": "mean", "field": "Horsepower", "type": "quantitative"}
  }
}
```

- `$schema` is the URN form `urn:prism:schema:v1:spec`. Schema bundle lives in `schema/v1/` (`//go:embed`'d into the binary). `prism init` writes the JSON Schema files into `.prism/schemas/` for editor autocomplete.
- `data` supplies rows as inline `values`, a runtime `ref` (resolved by a caller-supplied `DataResolver`), a `datasets`-block `name`, or a geodata `feature_collection`. The external `data.source` (`.pulse`) variant was removed in E4-S3 — Prism never reads `.pulse`; a `source` key is rejected at decode with `PRISM_SPEC_039`. Vega-Lite's `data.url` is **not** accepted either.
- `type` is required on every channel encoding (Prism is strict — Vega-Lite's inference is not implemented).
- **Inline column types come from the first _non-null_ value, not from row 0.** `table/inline.go` scans the rows per field and short-circuits at that value, so a leading `null` in a series (very common in real time-series data) no longer poisons the column's kind. Column order is the union of every row's keys, sorted alphabetically, so a field introduced by a later row still lands deterministically. A column that is null in every row has nothing to infer from and falls back to categorical/string — every row is flagged in its null bitmap, so the kind never holds a value. An explicit `data.fields` declaration bypasses inference entirely and always wins; a genuinely mixed column (a real string in a column inferred numeric) still raises `PRISM_RESOLVE_INLINE_TYPE_MISMATCH`.
- Vega-Lite's `params` / signals and per-encoding `condition` blocks are not implemented in v1.

Bump the schema bundle version (`schema/v1/` → `schema/v2/`) only on backwards-incompatible spec shape changes. Additive fields stay on `v1`. Bump triggers an update of `cmd/prism/cmd_init.go` (templates) and every `$schema` reference in `docs/src/`.

## Non-Skippable Gates

These tests live in `internal/gates/` and per-package `*_test.go` files. CI is configured to fail the build on any of them:

- **Format / vet / staticcheck** — `make fmt-check && make lint`. CI runs both jobs (`test` + `lint`) on every PR.
- **Race detector** — `make test-race`. Spec validation, plan execution, and the table cache are concurrent paths; the race detector catches data-race regressions before they ship.
- **Golden parity** — `render/svg/goldens_test.go` and `validate/golden_test.go` compare against committed SVG and JSON envelopes; mismatches fail the build. Regenerate via the per-package `-update` flag, never hand-edit.
- **Cross-impl parity (opt-in)** — `PRISM_CROSS_IMPL=1` enables host-Go-vs-TinyGo-wasm scene IR / SVG comparison under `internal/devtools/`. Off by default in CI (requires `node`); run locally before changes to `encode/scene/` or `render/svg/`.
- **TinyGo wasm build + size gate** — CI installs pinned TinyGo 0.41.1, runs `make build-wasm-tinygo`, and hard-gates `internal/gates/wasm_tinygo_size_test.go` (the TinyGo module is the sole browser artifact — gzipped under `PRISM_WASM_TINYGO_MAX_BYTES`, raw under `PRISM_WASM_TINYGO_RAW_MAX_BYTES`). Locally the gate skips cleanly when `tinygo` is not on `PATH`.
- **Smoke tests** — `cmd/prism/*_smoke_test.go` covers every CLI leaf end-to-end against fixtures. New CLI leaves require a smoke test.
- **Gallery freshness** — `docs/src/gallery/` SVGs are regenerated by `render/svg/goldens_test.go` outputs; gallery changes require a matching test fixture update.
- **Axis call-site gate** — `encode/axis_call_sites_test.go` parses every non-test file in `encode/` and fails if any `BuildAxisWithOpts` call passes a bare `DefaultAxisOpts(...)` (only `BuildAxis`, the documented default-opts wrapper, is allowlisted). It stops a call site from silently discarding a channel's `axis` block — the E3-S5 regression, which was invisible because `shared` is the default resolve mode for `layer` and `facet`.
- **MCP import firewall** — `internal/gates/mcp_firewall_test.go` asserts the SDK-free `mcp/` core never transitively imports an MCP SDK (`github.com/modelcontextprotocol/go-sdk` or `github.com/mark3labs/mcp-go`); the go-sdk binding stays quarantined outside the core, in `mcp/gosdk/` and `mcpserve/`. The companion `mcp/catalog_test.go` asserts every tool's input/output schema still reflects from struct tags via jsonschema-go.
- **Pulse-free import firewall** — `internal/gates/pulse_firewall_test.go` shells out to `go list -deps` over the whole host module (regular + `-test` graphs) and the `cmd/prismwasm` entry under `GOOS=js/GOARCH=wasm`, and fails if any dependency path matches `github.com/frankbardon/pulse` or its former expression engine `github.com/expr-lang/expr`. This locks in the eviction: the core never re-acquires Pulse (or expr-lang) on any platform.

## What NOT to Do

- **Do not put business logic in `cmd/prism/`.** CLI is a thin adapter — parse flags, construct library objects, call methods, format output. Smoke tests gate this discipline.
- **Do not bypass `afero.Fs`** for file access — defeats hermetic testing and the in-memory `prism serve` path.
- **Do not hand-edit golden files.** Regenerate via the per-package `-update` flag, and review the diff before committing.
- **Do not introduce an expression language.** `filter`, `calculate`, and condition `test` are structured JSON built-ins (`spec/predicate.go`, `spec/calc_expr.go`) — decode rejects a raw string outright. There is no `datum.` DSL, no Pulse-expression string, and no JS eval; do not add one. Extend the built-in grammar (new operator / function) via the spec union + schema `$def` + validate rule instead. Anything the grammar cannot express is precomputed by the caller upstream.
- **Do not import `service/` or `processing/` from `descriptor/`.** (Pulse rule; mirrored here: do not import `plan/` or `render/` from `validate/`, `compile/`, or `resolve/`. Predict / validate are no-execute by structural ban.)
- **Do not import an MCP SDK from the `mcp/` core.** The `mcp/` package is the SDK-free importable core (typed handlers + `ToolDescriptor` catalog), and a core consumer must be able to mount the catalog without acquiring an MCP SDK transitively — that is the invariant, and it is absolute. The `github.com/modelcontextprotocol/go-sdk` dependency lives in exactly two sanctioned packages *outside* the core: `mcp/gosdk/` (the adapter that mounts onto a caller-supplied server) and `mcpserve/` (the in-process runner, which delegates to `gosdk.Register`). Enforced by `internal/gates/mcp_firewall_test.go` (`go list -deps` over the core's import graph, including the `-test` graph) — it scopes to `mcp/` alone, which is precisely why a root-level `mcpserve/` is legitimate and why an SDK import inside `mcp/` (test files included) is not. New SDK-touching code goes in one of the two sanctioned packages, never in the core.
- **Do not `fmt.Sprintf` JSON or SVG.** Use `encoding/json` and the scene → SVG emitters in `render/svg/`. Hand-formatted output drifts from goldens within one release.
- **Do not skip `make fmt-check && make lint` before committing.** CI will reject the PR — fix locally first.
- **Do not defer doc updates to a follow-up PR.** The follow-up will not happen. Update `docs/src/`, `schema/v1/`, and this file in the same PR per the Update Demand.
- **Do not add a transform without registering a Plan node.** `spec.Transform*` decoders, the union dispatcher in `spec/transform_union.go`, and the `plan/nodes/<name>.go` file must land together. Tests in `plan/dag_test.go` and `plan/builder.go` will fail otherwise.
- **Do not read a sort direction by comparing the string.** `"ascending"` / `"descending"` are canonical and `"asc"` / `"desc"` are aliases; `spec.SortDirectionDescending` (via `plan/nodes.SortKey.Descending()`) is the only reader. Comparing against one literal is precisely how `"descending"` — the spelling the JSON Schema documents — silently sorted ascending in `compile/inmem/{sort,window}.go` until E5-S4.
- **Do not reorder rows at encode time.** `encoding.order` is resolved once, in the plan, as a synthetic `SortNode` (`plan/build`'s `injectEncodingOrder`); stack order, draw order and path point sequence all fall out of the resulting table order. The only sanctioned encode-side code is `marks.Inputs.Ordered`, which suppresses line/area's default x-sort. A second permutation at encode would give plan and encode two sources of truth for the same decision.
- **Do not add an env var without updating "Build / Env"** above. New ceilings also land their default constant and parser in `internal/limits/limits.go`.
